# Project Configuration
# ========================================
# SETUP WORKFLOW:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in the configuration values below
# 3. Run: bun run init (this will auto-generate the OIDC values)
# ========================================

# Project name (used for domains and branding)
PROJECT_NAME="nuxtship"
APPLICATION_NAME="NuxtShip"
VERSION="development"

# Domain Configuration (defaults to official/public nuxtship.dev subdomains)
APP_DOMAIN="app.nuxtship.dev"
AUTH_DOMAIN="auth.nuxtship.dev"
ADMIN_DOMAIN="admin.nuxtship.dev"
PROXY_DOMAIN="proxy.nuxtship.dev"

# Docker Configuration
DOCKER_NETWORK="nuxtship-dev"

# PostgreSQL Host Port
# Change this if port 5432 is already in use on your host
POSTGRES_HOST_PORT="5432"

# Application Database Configuration
APP_DB_NAME="nuxtship"
APP_DB_USER="nuxtship"
APP_DB_PASSWORD=""
APP_DB_ADMIN_PASSWORD=""

# Logto Configuration
LOGTO_DB_USER=""
LOGTO_DB_PASSWORD=""

# Logto SMTP Configuration (optional for email notifications)
LOGTO_SMTP_HOST=""
LOGTO_SMTP_PORT="587"
LOGTO_SMTP_USER=""
LOGTO_SMTP_PASSWORD=""
LOGTO_SMTP_FROM="auth@nuxtship.dev"
LOGTO_SMTP_FROMNAME="NuxtShip"

# Nuxt UI Pro License (optional)
NUXT_UI_PRO_LICENSE=""

# Logto Nuxt Module Configuration
# NUXT_LOGTO_COOKIE_ENCRYPTION_KEY: Random string for cookie encryption (openssl rand -hex 32)
NUXT_LOGTO_APP_ID=""
NUXT_LOGTO_APP_SECRET=""
NUXT_LOGTO_COOKIE_ENCRYPTION_KEY=""


# ========================================
# INFRASTRUCTURE VERSIONS - Only change if you need specific versions
# ========================================
# Default versions are tested and stable combinations
TRAEFIK_VERSION="v3.5.0"
LOGTO_VERSION="1.30.1"
POSTGRES_VERSION="17-alpine"

# ========================================
# AUTO-GENERATED - These will be set automatically by 'bun run init'
# ========================================

# Logto Authentication Configuration
# These values will be populated after running the provisioning script
# The NUXT_LOGTO_ENDPOINT will be set to https://${AUTH_DOMAIN}